# Contributing

Create an issue before large changes. Keep changes focused, add tests for behavior,
and run `make verify` before opening a pull request. KubeLore must remain usable
without cluster credentials and must never apply fixes to a cluster.

